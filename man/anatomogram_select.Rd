% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anatomogram_select.R
\name{anatomogram_select}
\alias{anatomogram_select}
\title{Browse, select, or filter anatomogram tissues -- by organ, by organ system}
\usage{
anatomogram_select(
  sex = c("male", "female"),
  organs = NULL,
  system = NULL,
  value_name = "value",
  outline = TRUE
)
}
\arguments{
\item{sex}{\code{"male"} or \code{"female"}.}

\item{organs}{\code{NULL} (default), or a character vector selecting specific
organs. Each element is either a literal \code{tissue_id} (e.g.
\code{"UBERON_0000948"}) or matched fuzzily (case-insensitive, partial)
against \code{tissue_name}. An element matching zero tissues emits a
\code{warning()} and is dropped; one matching more than one distinct
\code{tissue_id} (e.g. \code{"hippocampus"}, which has a left and a right entry
under separate IDs) is an error listing the candidates -- use a more
specific term or the literal \code{tissue_id}. Naming an element (e.g.
\code{c(kidney = "high")}) attaches that value under \code{value_name}; unnamed
elements are pure selection. Mutually exclusive with \code{system}.}

\item{system}{\code{NULL} (default), or a character vector of one or more
organ systems (see \link{organ_systems} for the full list, or call
\code{anatomogram_select(sex)} with \code{organs}/\code{system} both \code{NULL} to browse).
Unknown names error listing the valid ones. Mutually exclusive with
\code{organs}.}

\item{value_name}{Name of the column holding attached values in organ
mode (default \code{"value"}). Ignored in browse/system mode.}

\item{outline}{If \code{TRUE} (default), include the body outline rows too
(in system mode, once per requested system block), so the result can
drive both an outline layer and a filled-organs layer.}
}
\value{
If both \code{organs} and \code{system} are \code{NULL} (browse mode): a
lookup data frame of \code{tissue_id}, \code{tissue_name}, \code{system} (a tissue in
more than one system appears as more than one row). Otherwise: the
usual \code{hgMale}/\code{hgFemale} polygon columns filtered to the selection,
plus a \code{system} column (system mode) or a \code{value_name} column (organ
mode).
}
\description{
A single entry point for the three things you'd otherwise need separate
functions for: browsing what tissues/systems exist, pulling one or more
whole organ systems, or selecting specific organs (optionally attaching
a value to each, e.g. for \code{fill}). Output is always shaped for
\code{ggplot2::geom_polygon()} directly (see \code{examples/anatomogram_values_demo.R},
\code{examples/anatomogram_system_demo.R}) -- no \code{geom_map()} join needed.
}
\examples{
anatomogram_select("male")                              # browse
anatomogram_select("male", system = "Skeletal")          # all bones
anatomogram_select("male", organs = c(kidney = "high"))  # one organ, valued

}
